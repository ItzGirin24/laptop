rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all access for development (both authenticated and unauthenticated)
      allow read, write: if true;
    }
  }
}
